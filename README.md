# kieran.casa

Source for [kieran.casa](https://kieran.casa), a Jekyll site.

## Setup

Install Ruby gems and Node packages:

```
bundle install
npm install
```

## Running locally

Serve the site with drafts and live reload:

```
bundle exec jekyll serve --drafts --livereload
```

Then open <http://localhost:4000>.
