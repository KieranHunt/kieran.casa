version: 0.1
backend:
  phases:
    build:
      commands:
        - amplifyPush --simple
        - echo "👌🏻"
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands: []
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
